[package]
name = "fr-media"
version = "0.1.0"
description = "FrankenRemote media contracts and bounded encoded-picture delivery"
edition.workspace = true
license-file.workspace = true
repository.workspace = true

[dependencies]
# First-party types and actual packet codecs only. Runtime and foreign media
# implementations remain outside this dependency boundary.
fr-core.workspace = true
fr-wire.workspace = true

[features]
# Test double only; release capability never follows from enabling this flag.
testing = []

[lints]
workspace = true
