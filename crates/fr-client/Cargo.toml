[package]
name = "fr-client"
version = "0.1.0"
edition.workspace = true
license-file.workspace = true
repository.workspace = true
publish = false

[dependencies]
fr-core.workspace = true
fr-wire.workspace = true
fr-media.workspace = true
serde = { workspace = true, features = ["derive"] }
serde_json.workspace = true

[dev-dependencies]
# Placeholder codecs for this crate's own tests only; dev-dependency features
# never reach a non-test build of fr-client.
fr-media = { workspace = true, features = ["testing"] }

[lints]
workspace = true
