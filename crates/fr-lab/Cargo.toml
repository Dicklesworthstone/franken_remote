[package]
name = "fr-lab"
version = "0.1.0"
description = "Bounded deterministic fault scenarios for FrankenRemote production state machines"
edition.workspace = true
license-file.workspace = true
repository.workspace = true
publish = false

[dependencies]
asupersync.workspace = true
fr-core.workspace = true

[dev-dependencies]
fr-client.workspace = true
# Lab scenarios drive the placeholder audio codecs and virtual-mic endpoint.
fr-media = { workspace = true, features = ["testing"] }
fr-wire.workspace = true

[lints]
workspace = true
